name: Deploy Application

on:
  push:
    branches:
      - main # 배포할 브랜치를 지정하세요 (예: main 또는 feat/image)

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy Application via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            echo "Test Deployment Script"
            # cd /path/to/your/project  # 서버에서 프로젝트가 위치한 절대 경로로 변경하세요
            # git pull origin main  # 브랜치를 맞춰서 변경하세요
            # docker build -t carpro-app .
            # docker stop carpro-app || true
            # docker rm carpro-app || true
            # docker run -d --name carpro-app -p 3000:3000 carpro-app
